query AdminGetUsersPageData {
    users {
        findMany(request: {}) {
            userId
            firstName
            lastName
            profilePictureUrl
            isCook
            createdAt
        }
    }
}
