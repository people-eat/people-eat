query AdminGetCooks {
    cooks {
        findMany(request: {}) {
            cookId
            user {
                firstName
                lastName
                profilePictureUrl
            }
            isVisible
            city
        }
    }
}
