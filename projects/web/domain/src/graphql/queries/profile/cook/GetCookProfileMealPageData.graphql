query GetCookProfileMealPageData($cookId: String!, $mealId: String!) {
    sessions {
        current {
            cookieSettings {
                ...CookieSettings
            }
            user {
                ...SignedInUser
            }
        }
    }
    cooks {
        meals(cookId: $cookId) {
            findOne(mealId: $mealId) {
                mealId
                title
                description
                imageUrl
                type
                createdAt
            }
        }
    }
}
