mutation CreateBookingRequestByGlobalBookingRequestId($globalBookingRequestId: String!, $cookId: String!) {
    cooks {
        bookingRequests(cookId: $cookId) {
            success: createOne(globalBookingRequestId: $globalBookingRequestId)
        }
    }
}
