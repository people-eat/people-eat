import type { Meta } from '@storybook/react';
import { PEProfileNavigation } from './PEProfileNavigation';

const meta: Meta<typeof PEProfileNavigation> = {
    component: PEProfileNavigation,
    title: 'PEProfileNavigation',
};

export default meta;

export const Primary = {
    args: {},
};
